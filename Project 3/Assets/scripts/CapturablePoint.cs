using UnityEngine;
using System.Collections;

public class CapturablePoint : MonoBehaviour {
	public enum CapturableType{
		Planet,
		Station
	}

	public CapturableType type;

	// Use this for initialization
	void Start () {
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
